import '../../App.css';

const Quote = () => (
    <div className="quote-card__container">
        <p className="quote-card__quote-text">
            The binary gap of a number is defined as the maximum number 
            of consecutive zeros that occur between two ones in the binary representation of the number.
        </p>
    </div>
)

export default Quote;